import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.io.ionic.angular.two',
  appName: 'ionic-angular-app-2',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
